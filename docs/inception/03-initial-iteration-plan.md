# 初始迭代计划 -- 灵知 (Gnosis)

> 启动阶段交付物 3/3：编排后续迭代与里程碑。

## 阶段-迭代映射

| RUP 阶段 | 迭代 | 负责人 | 目标 | 退出标准 |
|---|---|---|---|---|
| inception | 启动范围确认 | 规划者 | 范围、目标、约束与初始风险确认 | 三份工件 + 本文件 |
| elaboration | 架构与风险细化 | 规划者 | 形成可执行的架构基线、依赖排序、构建迭代计划 | 架构基线 + 风险更新 + 迭代协议 |
| construction-1 | 数据与报表开发迭代 | 数据与报表开发者 | `DataSource` 接口 + Elasticsearch/PostgreSQL/MySQL/向量数据库 4 类适配器 | 接口稳定 + 4 类适配器可工作 + 评估通过 |
| construction-2 | 文件与同步开发迭代 | 文件与同步开发者 | Excel/Word/PDF/Markdown 解析器 + 切片 + 同步队列 | 4 类解析器 + 切片器 + 同步队列 + 评估通过 |
| construction-3 | AI 与智能体开发迭代 | AI 与智能体开发者 | embedding 服务（BGE-M3）+ 端到端流水线 | embedding 可调用 + 端到端 demo + 评估通过 |
| construction-4 | 多端体验开发迭代 | 多端体验开发者 | Electron 主进程 + React 渲染层 + IPC + 状态机 | 桌面端可启动、可上传、可检索 + 评估通过 |
| transition | 移交验收 | 文档与交接负责人 | 最终验收 + 文档 + 已知问题 | 文档齐备 + 交接清单 + 评估通过 |

## 启动阶段退出条件（已达成）

- [x] 范围与目标已确认（`01-project-scope.md`）。
- [x] 初始风险已列出（`02-initial-risks.md`）。
- [x] 后续迭代计划已编排（本文件）。

## 下一动作

1. 规划者进入 elaboration 迭代：架构基线、风险更新、构建迭代协议。
2. 开发者按 construction-1..4 顺序交付，每个迭代协议由规划者制定。
3. 评估者按迭代协议与退出标准复核，发现问题反馈对应开发者。
4. 文档与交接负责人最后产出 README、API、运行说明与已知问题。